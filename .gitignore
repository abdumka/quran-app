# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json
# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
 
*.zip
backups/
temp_quran_repo/
android/.gradle/
# One-time Hafs text download cache for generate_narration_map.py
hafs_uthmani.json

# al-Naihi mirror download output (push to GitHub mirror, do not commit to app repo)
tools/naihi_verses/

tools/_qaloun_data.json
tools/naihi_durations.json

# Cloudflare R2 credentials (never commit)
tools/.r2_secret

# Tafsir build cache: source surah files fetched by tools/build_tafsir_pages.py.
# The bundled output (assets/data/ar.muyassar.json) IS committed; the online
# per-page output lives under /build/tafsir and is uploaded to R2, not committed.
.tafsir_tmp/

# Zad al-Masir source material (Shamela HTML export + OCR review build): build
# input for tools/build_zad_almasir.py, not app assets — keep out of the repo.
assets/temp/
