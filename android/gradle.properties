org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
# Optimized resource shrinking — integrates resource shrinking into R8's
# optimization pipeline instead of running the legacy standalone shrinker.
# Opt-in for AGP 8.6-8.x (we're on 8.11.1); default from AGP 9.0, so this line
# can be dropped after that upgrade. Requested by Play Console's app
# optimization insights. Pairs with isMinifyEnabled/isShrinkResources in
# app/build.gradle.kts — it is a no-op without them.
android.r8.optimizedResourceShrinking=true
